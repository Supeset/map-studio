# AI

当前为[map-studio]，前端的项目
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
使用主题模式
使用响应式布局
严格就按我的代码风格写
请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答



### with new code

再添加一个数据源，是火箭回收点数据源
读取这个网址 /rocket/landing_sites.json
其数据格式如下：
[
  {
    "record_id": "1",
    "latitude": 103.48072481551263,
    "longitude": 38.44514633002117,
    "name": "ZQ-3 LZ",
    "decription": "ZhuQue-3 Landing Site",
    "country": "🇨🇳中国"
  },
  {
    "record_id": "2",
    "latitude": 101.91859255030832,
    "longitude": 39.05052326177285,
    "name": "CZ-12A LZ",
    "decription": "Long March 12A Landing Site",
    "country": "🇨🇳中国"
  }
]
也同样添加一个Panel，展示这个数据源

### history

优化一下这个页面rocket.vue 
数据更换为读取 /rocket/enriched_locations.json 
其数据格式如下：
[
  {
    "record_id": "rec31hdIS4",
    "latitude": 25.9962,
    "longitude": -97.154423,
    "name": "Pad 1",
    "location_name_en": "Pad 1, Starbase, Texas, USA",
    "country": "🇺🇸美国",
    "launch_center": "SpaceX星港",
    "coordinates": "-97.154423,25.9962"
  },
]
在地图上展示 name
点击出现的popup 中展示 name, location_name_en, country, launch_center, coordinates
同时使用 ThePanel.vue 展示这个列表，
这个列表按 country 分组，分组可以展开收起
点击列表中的元素，可以flyto并展示 popup

### history

改一下 coords.vue 中的 Spotlight Search Box 样式与放置位置，改为与 GeoJsonPanel.vue 类似的样式宽度以及位置，可以展开与收起。再添加一个可以记录历史记录的功能。可以将 GeoJsonPanel.vue 外面的框抽离出来，与其共用

### history

为啥从其他路由进入到 /draw 路由的时候，点击添加点/线/面，会下面报错，但如果在这个页面重新刷新一下就没问题

Uncaught TypeError: Cannot read properties of null (reading 'add')
    at ModeInterface.addFeature (@mapbox_mapbox-gl-draw.js?v=45f53df0:931:26)
    at DrawLineString.onSetup (@mapbox_mapbox-gl-draw.js?v=45f53df0:3491:10)
    at Object.start (@mapbox_mapbox-gl-draw.js?v=45f53df0:1050:22)
    at ModeHandler (@mapbox_mapbox-gl-draw.js?v=45f53df0:582:14)
    at Object.changeMode (@mapbox_mapbox-gl-draw.js?v=45f53df0:1224:19)
    at api.changeMode (@mapbox_mapbox-gl-draw.js?v=45f53df0:3771:16)
    at Proxy.setDrawMode (draw.vue:293:22)
    at _createElementVNode.onClick._cache.<computed>._cache.<computed> (draw.vue:494:19)
    at callWithErrorHandling (runtime-core.esm-bundler.js?v=45f53df0:197:19)
    at callWithAsyncErrorHandling (runtime-core.esm-bundler.js?v=45f53df0:204:17)

### history

给 这个页面 /draw 添加上从路径参数上读取 geojson 数据，并展示出来
类似 geojson.io 这个网站一样 http://geojson.io/#data=data:application/json,%7B%22type%22%3A%22LineString%22%2C%22coordinates%22%3A%5B%5B0%2C0%5D%2C%5B10%2C10%5D%5D%7D


### history

添加一个geojson数据展示功能，类似下面这样，不可编辑。可以与 Properties Panel 放在一块，平常就展示，选中某个元素时，可以展示其属性。同时这个panel支持收起与展开

### history

修复一下 app/pages/draw.vue 中 v-if="key.toString().includes('color')" 的判断
应该 marker-color stroke stroke fill 都是 出现颜色选择框

### history

新建一个页面路由，专门用来在地图上画图，可以画，点，线，多边形，矩形，圆形，
双击结束绘制，将数据存在 localStorage里中（这里使用vueuse的useLocalStorage）
点击对应的元素，可以popup展示其属性，使用表格展示，可以添加与删除属性，同时可以支持一键添加默认样式属性
对应的 点有 marker-color marker-size marker-symbol
线有 stroke stroke-width stroke-opacity
面有 stroke stroke-width stroke-opacity fill fill-opacity

### history

将 app/components/TheHeader.vue 中的功能按钮组放在 Map 组件中，使每个页面都支持查看关于，切换主题色，以及更改地图底图。
同时改进这个功能按钮组，
首页将其放在地图的右下角，
添加更多功能，比如定位当前设备位置，以及切换地图底图（这个可以更换一个icon）。

### history

帮我创建一个新的路由页面，使用展示地图上的所有火箭发射点
数据我已经放在了 public/rocket/pads.json
数据格式如下，先不用管 statuses。因为火箭发射点可能会很密集，你需要使用 cluster 功能
