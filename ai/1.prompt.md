# AI

当前为[map-studio]，前端的项目
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
使用主题模式
使用响应式布局
严格就按我的代码风格写
请不要使用传统的diff + / - 格式。请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答


```bash
python3 ./ai/print_project_files.py . "\.(vue|ts|js|json|toml|md)$|Dockerfile$|eslint\.config\.js$" -o ./ai/project_context.txt -e "node_modules,.git,.nuxt,dist,build,public,pnpm-lock.yaml,.vscode,ai,.output"
```

创建一个新路由页面。
同样的放在内部工具里
实现的功能是：
打开这个页面展示一个类似"Spotlight" 可以指苹果设备上内置的搜索功能的样式的输入框
然后里面可以输入类似 122.20796772837653,29.99769125778746 这样的坐标内容
然后在地图上展示这个坐标，并fly到该坐标
同时用 gcoord 转为 GCJ02 以及 BD09 坐标，并展示在页面上，使用不同的颜色表示。
同时在 输入框 下面展示几个按钮，分别用与将该坐标作为 GCJ02 与 BD09，对应的在地图上展示 BD09 与 WGS84 坐标系的点，与对应的在地图上展示 GCJ02 与 WGS84 坐标系的点。