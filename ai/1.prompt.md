# AI

当前为[map-studio]，前后端集成的项目
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
使用主题模式
使用响应式布局
严格就按我的代码风格写
请不要使用传统的diff + / - 格式。请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答


```bash
python3 ./ai/print_project_files.py . "\.(vue|ts|js|json|toml|md)$|Dockerfile$|eslint\.config\.js$" -o ./ai/project_context.txt -e "node_modules,.git,.nuxt,dist,build,public,pnpm-lock.yaml,.vscode,ai,.output"
```

创建一个新页面路由，用作天文相关的信息查看。如何进入呢？通过ExternalLinks.vue里的内部工具打开，当然现在的ExternalLinks.vue需要重构一下。
有什么功能？
1. 点击地图上的某个点，可以获取它，当日的太阳升起与落下的方向角，以及太阳的升起、落下和凌日具体时间，当然要考虑用户当前的时区。
2. 用到的npm 包有   dayjs 以及 astronomy-bundle